import PropTypes from "prop-types";

function HomePresenter() {
    return (
        <div>
            초기 홈 화면
        </div>
    )
}

HomePresenter.propTypes = {
    // ex) prop: PropTypes.type,
};

export default HomePresenter;