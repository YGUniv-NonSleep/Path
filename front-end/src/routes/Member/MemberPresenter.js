import PropTypes from "prop-types";

function MemberPresenter() {
    return (
        <div>
            <h2>초기 회원 화면</h2>
        </div>
    )
}

MemberPresenter.propTypes = {
    // ex) prop: PropTypes.type.isRequired,
};

export default MemberPresenter;