import styled from "styled-components";

const StyledMap = styled.div`
    width: 100%;
    height: 100vh;
`;

function Map() {
    return (
        <StyledMap id="map"></StyledMap>
    )
}

export default Map;